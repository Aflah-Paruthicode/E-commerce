<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Portal | DashFootwares</title>
    <link rel="icon" type="image/x-icon" href="/logo/DashFootwares.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <style>
        :root {
            --admin-dark: #0f172a;
            --admin-accent: #334155;
            --error-red: #ef4444;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: radial-gradient(circle at top right, #1e293b, #0f172a);
            height: 100vh;
            margin: 0;
            display: flex;
            flex-direction: column;
        }

        /* Subtle Admin Header */
        header {
            padding: 20px 0;
            background: transparent;
        }

        .logo-box {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
        }

        .logo-box img { width: 35px; filter: brightness(0) invert(1); }
        .logo-box h3 { 
            color: #fff; 
            font-size: 1rem; 
            font-weight: 800; 
            letter-spacing: 2px; 
            margin: 0;
        }

        /* Login Container */
        main {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .admin-login-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            width: 100%;
            max-width: 400px;
            padding: 50px 40px;
            border-radius: 24px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        }

        .admin-login-card h2 {
            color: #fff;
            font-weight: 800;
            font-size: 24px;
            text-align: center;
            margin-bottom: 8px;
            letter-spacing: -0.5px;
        }

        .admin-login-card p {
            color: #94a3b8;
            text-align: center;
            font-size: 13px;
            margin-bottom: 35px;
        }

        /* Input Styling */
        .input-wrapper {
            margin-bottom: 20px;
            position: relative;
        }

        .input-wrapper label {
            display: block;
            color: #cbd5e1;
            font-size: 12px;
            font-weight: 600;
            margin-bottom: 8px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .input-wrapper input {
            width: 100%;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 12px 15px;
            border-radius: 12px;
            color: #fff;
            outline: none;
            transition: 0.3s;
        }

        .input-wrapper input:focus {
            border-color: #64748b;
            background: rgba(255, 255, 255, 0.08);
            box-shadow: 0 0 0 4px rgba(255, 255, 255, 0.02);
        }

        /* Error Message */
        .error-text {
            color: var(--error-red);
            font-size: 12px;
            font-weight: 600;
            margin-bottom: 20px;
            text-align: center;
            background: rgba(239, 68, 68, 0.1);
            padding: 10px;
            border-radius: 8px;
        }

        /* Submit Button */
        .admin-btn {
            width: 100%;
            background: #fff;
            color: var(--admin-dark);
            border: none;
            padding: 14px;
            border-radius: 12px;
            font-weight: 700;
            font-size: 14px;
            text-transform: uppercase;
            transition: 0.3s;
            cursor: pointer;
            margin-top: 10px;
        }

        .admin-btn:hover {
            background: #cbd5e1;
            transform: translateY(-2px);
        }

        .footer-tag {
            text-align: center;
            color: #475569;
            font-size: 11px;
            margin-top: 30px;
        }
    </style>
</head>

<body>

    <header>
        <div class="container">
            <div class="logo-box">
                <img src="/logo/DashFootwares.png" alt="Logo">
                <h3>DASH CONTROL</h3>
            </div>
        </div>
    </header>

    <main>
        <div class="admin-login-card">
            <h2>Admin Login</h2>
            <p>Enter your credentials to access the dashboard</p>

            <form action="" method="post">
                <div class="input-wrapper">
                    <label>Email Address</label>
                    <input type="email" name="email" placeholder="admin@dash.com" required>
                </div>

                <div class="input-wrapper">
                    <label>Password</label>
                    <input type="password" name="password" placeholder="••••••••" required>
                </div>

                <% if( typeof emessage !== 'undefined' ) { %>
                    <div class="error-text">
                        <i class="fa-solid fa-circle-exclamation me-2"></i> <%= emessage %>
                    </div>
                <% } %>

                <button type="submit" class="admin-btn">Secure Login</button>
            </form>

            <div class="footer-tag">
                &copy; 2026 Dash Footwares Management System
            </div>
        </div>
    </main>

</body>
</html>