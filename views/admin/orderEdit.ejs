<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Order | DashControl</title>
    <link rel="icon" type="image/x-icon" href="/logo/DashFootwares.png" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="/css/adminCss/products.css" />  
     <link rel="stylesheet" href="/css/adminCss/orderEdit.css">

  </head>
  <body>
    <header>
      <div class="d-flex align-items-center">
        <img src="/logo/DashFootwares.png" alt="" width="35" />
        <h3 class="ms-3 mb-0" style="font-size: 16px; font-weight: 800">DASH CONTROL</h3>
      </div>
      <div class="d-flex align-items-center gap-3">
        <span class="small fw-bold me-2">Aflah (Admin)</span>
        <form action="/admin/logout"><button type="submit" class="btn btn-sm btn-outline-danger fw-bold">LOGOUT</button></form>
      </div>
    </header>

    <aside class="sidebar">
      <a href="/admin/dashboard" class="nav-btn"> <i class="fa-solid fa-chart-line"></i><span>Dashboard</span> </a>
      <a href="/admin/customers" class="nav-btn"> <i class="fa-solid fa-users"></i><span>Customers</span> </a>
      <a href="/admin/product" class="nav-btn"> <i class="fa-solid fa-shoe-prints"></i><span>Products</span> </a>
      <a href="/admin/orders" class="nav-btn active"> <i class="fa-solid fa-bag-shopping"></i><span>Orders</span> </a>
      <a href="/admin/category" class="nav-btn"> <i class="fa-solid fa-layer-group"></i><span>Categories</span> </a>
      <a href="/admin/couponManagement" class="nav-btn"> <i class="fa-solid fa-ticket"></i><span>Coupons</span> </a>
      <a href="/admin/banner" class="nav-btn"> <i class="fa-solid fa-image"></i><span>Banners</span> </a>
      <a href="/admin/offerManagement" class="nav-btn"><i class="fa-solid fa-percent"></i><span>Offers</span></a>
      <a href="/admin/salesReport" class="nav-btn"> <i class="fa-solid fa-file-invoice-dollar"></i><span>Sales Report</span> </a>
    </aside>

    <main class="main-content">
      <div class="action-bar">
        <h4 class="fw-bold m-0">Order Management</h4>
        <a href="/admin/orders" class="btn btn-sm btn-outline-secondary"> <i class="fa-solid fa-arrow-left me-2"></i>Back to Orders</a>
      </div>

      <% if (locals.emessage) { %>
      <div class="alert alert-danger py-2 px-3 mb-4 rounded-3 small fw-bold"><i class="fa-solid fa-triangle-exclamation me-2"></i><%= emessage %></div>
      <% } %>

      <div class="content-card p-4">
        <form action="/admin/editOrder/?order_id=<%= order._id %>" method="post">
          <div class="status-card p-3 rounded-3 mb-4">
            <div class="row align-items-end">
              <div class="col-md-8">
                <label class="form-label text-dark">Current Order Status</label>
                <select name="status" class="form-select fw-bold border-dark">
                  <option value="<%= order.status %>" selected>Current: <%= order.status %></option>

                  <% if(order.status == 'Return Requested') { %>
                  <option value="Return Rejected">Return Rejected</option>
                  <option value="Return Approved">Return Approved</option>
                  <% } else if(order.status == 'Return Approved') { %>
                  <option value="Return Completed">Return Completed</option>
                  <% } %>

                  <option value="Order Placed">Order Placed</option>
                  <option value="Packed">Packed</option>
                  <option value="Shipped">Shipped</option>
                  <option value="Delivered">Delivered</option>
                  <% if (order.status !== 'Delivered') { %>
                  <option value="Cancelled" class="text-danger">Cancelled</option>
                  <% } %>
                </select>
                <% if(order.reasonForReturn) { %>
                <div class="mt-2 small text-danger fw-bold">Return Reason: <%= order.reasonForReturn %></div>
                <% } %>
              </div>
              <div class="col-md-4">
                <button type="submit" class="btn-update">UPDATE STATUS</button>
              </div>
            </div>
          </div>

          <div class="row g-4">
            <div class="col-md-6">
              <div class="section-title"><i class="fa-solid fa-user"></i> Customer Information</div>
              <div class="mb-3">
                <label class="form-label">Customer Name</label>
                <input type="text" class="form-control readonly-box" readonly value="<%= user.name %>" />
              </div>
              <div class="mb-3">
                <label class="form-label">Mobile Number</label>
                <input type="text" class="form-control readonly-box" readonly value="<%= user.mobile %>" />
              </div>
              <div class="mb-3">
                <label class="form-label">Shipping Address</label>
                <textarea class="form-control readonly-box" readonly rows="2"><%= user.address %></textarea>
              </div>
            </div>

            <div class="col-md-6">
              <div class="section-title"><i class="fa-solid fa-circle-info"></i> Order Summary</div>
              <div class="row g-3">
                <div class="col-6">
                  <label class="form-label">Delivery Date</label>
                  <input type="text" class="form-control readonly-box" readonly value="<%= order.date %>" />
                </div>
                <div class="col-6">
                  <label class="form-label">Payment Method</label>
                  <input type="text" class="form-control readonly-box" readonly value="<%= order.paymentMethod %>" />
                </div>
                <div class="col-6">
                  <label class="form-label">Delivery Charge</label>
                  <input type="text" class="form-control readonly-box" readonly value="₹ <%= order.deliveryChaerge %>" />
                </div>
                <div class="col-6">
                  <label class="form-label">Total Amount</label>
                  <input type="text" class="form-control readonly-box fw-bold text-success" readonly value="₹ <%= order.totelAmmount %>" />
                </div>
                <div class="col-12">
                  <label class="form-label">Coupon/Discount</label>
                  <input
                    type="text"
                    class="form-control readonly-box"
                    readonly
                    value="<%= (order.coupon_applied !== 'no') ? order.coupon_applied : 'None applied' %>"
                  />
                </div>
              </div>
            </div>

            <div class="col-12">
              <hr class="my-2 text-muted" />
              <div class="section-title mt-3"><i class="fa-solid fa-box"></i> Product Details</div>
              <div class="row align-items-center bg-light p-3 rounded-3 border">
                <div class="col-md-8">
                  <div class="fw-bold text-dark"><%= product.name %></div>
                  <div class="small text-muted">Product ID: <%= order._id %></div>
                </div>
                <div class="col-md-4 text-md-end">
                  <span class="badge bg-dark px-3 py-2">Quantity: <%= order.quantity %></span>
                </div>
              </div>
            </div>
          </div>
        </form>
      </div>
    </main>
  </body>
</html>
