<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Join Us | DashFootwares</title>
    <link rel="icon" type="image/x-icon" href="/logo/DashFootwares.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;800&display=swap" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="/css/userCss/registration.css" />
  </head>

  <body>
    <header>
      <div class="container">
        <nav>
          <div class="logo">
            <a href="/" style="text-decoration: none; display: flex; align-items: center; color: inherit">
              <img src="/logo/DashFootwares.png" alt="Logo" />
              <h3>DASH FOOTWARES</h3>
            </a>
          </div>
          <ul class="navbar">
            <li><a href="/login" style="text-decoration: none; color: #1a1a1a; font-weight: 600">LOGIN</a></li>
          </ul>
        </nav>
      </div>
    </header>

    <main class="login-wrapper">
      <div class="login-card">
        <div class="brand-side">
          <h1>Step into the Future.</h1>
          <p>Create an account to track orders, save items to your wishlist, and get personalized recommendations.</p>
        </div>

        <div class="form-side">
          <h2>Create Account</h2>
          <p class="subtitle">Join the Dash community today.</p>

          <a href="/auth/google" class="google-btn">
            <img src="/signupWith/google (1).png" width="18" alt="" />
            Sign up with Google
          </a>

          <div class="divider">Or register with email</div>

          <form action="/register" method="post">
            <div class="input-group-custom">
              <i class="fa-solid fa-user"></i>
              <input type="text" name="name" placeholder="Full Name" required />
            </div>

            <div class="input-group-custom">
              <i class="fa-solid fa-envelope"></i>
              <input type="email" name="email" placeholder="Email Address" required />
            </div>

            <div class="input-group-custom">
              <i class="fa-solid fa-phone"></i>
              <input type="number" name="mno" placeholder="Mobile Number" required />
            </div>

            <div class="input-group-custom">
              <i class="fa-solid fa-lock"></i>
              <input type="password" name="password" placeholder="Password" required />
            </div>

            <button type="submit" class="l-btn">Register</button>

            <div class="login-prompt">Already have an account? <a href="/login">Login here</a></div>
          </form>
        </div>
      </div>
    </main>

    <%- include('../layouts/footer.ejs') %> <% if (locals.message) { %>
    <script>
      Swal.fire({
        title: "Success!",
        text: "<%= message %>",
        icon: "success",
        confirmButtonColor: "#1a1a1a",
      });
    </script>
    <% } %> <% if (locals.emessage) { %>
    <script>
      Swal.fire({
        title: "Registration Failed",
        text: "<%= emessage %>",
        icon: "error",
        confirmButtonColor: "#d33",
      });
    </script>
    <% } %>
  </body>
</html>
